# Core runtime
fastmcp>=0.9
langgraph>=0.2
langchain-core>=0.2
PyGithub>=2.3
langchain-mcp-adapters
mcp

# Development / testing (optional — install with: pip install -r requirements-dev.txt)
# pytest>=8.0
# pytest-asyncio>=0.23
# ruff>=0.4
# mypy>=1.10
